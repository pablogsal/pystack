[package]
name = "framehop_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
framehop = { path = "/Users/pablogsal/github/framehop" }

[profile.release]
lto = true
panic = "abort"
